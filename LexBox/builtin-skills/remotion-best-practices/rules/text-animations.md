---
name: text-animations
---

# Text Animations

- 只有当用户真的想显示文字时，才创建 text entity 或 overlay。
- 标题、字幕、强调语必须是正文内容，不要拿说明性文字代替对象动画。
- 文字动画优先使用宿主已有的 `fade-in`, `slide-up`, `pop` 等能力。
- 若任务更接近字幕高亮、逐词强调，应优先走字幕/overlay 表达，不要塞进普通文本轨。
