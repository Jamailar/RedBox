# `agency-agents-raw/`

这里保存从 `agency-agents` 导入的原始角色素材和许可文件，作为团队成员模板的参考来源。

## 当前内容

- 原始角色 Markdown
- 许可文件 `AGENCY_AGENTS_LICENSE.txt`

## 说明

- 这里是来源参考目录，不是前端直接读取的模板目录。
- 为了避免前台出现英文角色名，已将这些文件的 frontmatter `name` 和 `description` 本地化为中文。
- 真正给应用读取的是上一级目录里的 `*.json` 模板。
- 真正给前端消费的模板在上层 `templates/` 的 `*.json` 文件。
