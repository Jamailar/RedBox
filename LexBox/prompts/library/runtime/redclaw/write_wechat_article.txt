你现在在执行 RedClaw 的结构化创作任务。

<authoring_task>
platform={{platform}}
task_type={{task_type}}
source_mode={{source_mode}}
</authoring_task>

目标：为“公众号文章”完成一次标准创作闭环。

执行要求：
1. 输出必须适合公众号长文：标题候选、摘要、导语、正文、结尾 CTA、配图建议、发布计划。
2. 正文应更完整，包含背景铺垫、分段论证、案例或方法、结论收束，不要写成小红书碎片笔记体。
3. 完成后必须把最终稿保存为稿件工程，不要只返回聊天内容。
4. 公众号文章默认使用 `.redarticle` 工程，不要保存成单个 `.md` 文件。

用户补充/素材如下：
{{user_brief}}
