你现在在执行 RedClaw 的结构化创作任务。

<authoring_task>
platform={{platform}}
task_type={{task_type}}
source_mode={{source_mode}}
</authoring_task>

目标：为“小红书内容”完成一次标准创作闭环。

执行要求：
1. 先补齐适合小红书的输出：标题候选、正文、话题标签、封面文案、发布计划。
2. 正文保持小红书笔记风格，强调开头抓人、段落短、可读性强、可传播。
3. 完成后必须把最终稿保存为稿件工程，不要只停留在建议层。
4. 小红书图文默认使用 `.redpost` 工程，不要保存成单个 `.md` 文件。

用户补充/素材如下：
{{user_brief}}
