{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the RedBox desktop window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
